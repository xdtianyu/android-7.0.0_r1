These android-{1,2,3}/android.jar are used by api-generator
to compute api-versions.xml.

These are referenced by development/build/Android.mk

Note that API >= 4 are taken from prebuilts/sdk/ because
we already have "official" versions there.

