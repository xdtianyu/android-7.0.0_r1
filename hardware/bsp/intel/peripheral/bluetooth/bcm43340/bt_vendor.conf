# this is default configuration for bcm combo on intel products
# UART device port where Bluetooth controller is attached
UartPort = /dev/ttyMFD0

# Firmware patch file location
FwPatchFilePath = /system/vendor/firmware/bcm43340
